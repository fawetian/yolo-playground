# 05 - YOLO 训练 🏋️

## 本模块内容

学习如何使用自定义数据集训练 YOLO 模型。

## 子目录

| 目录 | 内容 | 预计时间 |
|-----|------|---------|
| `01_dataset_prep/` | 数据集准备 | 2 天 |
| `02_custom_training/` | 自定义训练 | 3 天 |
| `03_hyperparameter/` | 超参数调优 | 2 天 |
| `04_model_export/` | 模型导出 | 1 天 |

## 学习目标

完成本模块后，你将掌握：

- [ ] 准备 YOLO 格式的数据集
- [ ] 使用标注工具标注图像
- [ ] 训练自定义目标检测模型
- [ ] 调整超参数优化模型
- [ ] 导出模型为不同格式

## 训练流程

```
1. 数据收集 → 2. 数据标注 → 3. 数据集划分 → 4. 配置 YAML → 5. 训练 → 6. 验证 → 7. 导出
```

## macOS MPS 训练

```python
from ultralytics import YOLO

model = YOLO("yolo11n.pt")
model.train(
    data="data.yaml",
    epochs=100,
    device="mps",  # Apple Silicon GPU
    batch=8,       # MPS 建议较小的 batch
)
```

## 注意事项

### Apple Silicon 内存限制
MPS 共享系统内存，建议：
- batch size: 8-16
- imgsz: 640 或更小
- 关闭不必要的应用

### 遇到内存不足
```python
# 减小 batch
model.train(data="data.yaml", batch=4)

# 或使用 CPU
model.train(data="data.yaml", device="cpu")
```

## 运行示例

```bash
conda activate yolo
python 02_custom_training/train_custom_model.py
```

## 下一步

完成后进入 `06_integration/` 学习综合应用。

