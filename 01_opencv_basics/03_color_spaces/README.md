# 03_color_spaces - 颜色空间 🎨

## 学习目标

- 理解 BGR、RGB、HSV、灰度等颜色空间
- 进行颜色空间转换
- 基于颜色的图像分割

## 核心概念

### 常用颜色空间

| 空间 | 说明 | 用途 |
|-----|------|------|
| BGR | OpenCV 默认，蓝绿红顺序 | 图像读写 |
| RGB | 红绿蓝顺序 | Matplotlib 显示 |
| HSV | 色相-饱和度-明度 | 颜色检测/分割 |
| GRAY | 灰度 | 边缘检测等 |
| LAB | 亮度-色彩 | 颜色校正 |

### HSV 颜色范围

| 颜色 | H 范围 | S 范围 | V 范围 |
|-----|--------|--------|--------|
| 红色 | 0-10, 160-180 | 100-255 | 100-255 |
| 绿色 | 35-85 | 100-255 | 100-255 |
| 蓝色 | 100-130 | 100-255 | 100-255 |

## 核心 API

### 颜色转换
```python
# BGR -> RGB
rgb = cv2.cvtColor(img, cv2.COLOR_BGR2RGB)

# BGR -> HSV
hsv = cv2.cvtColor(img, cv2.COLOR_BGR2HSV)

# BGR -> 灰度
gray = cv2.cvtColor(img, cv2.COLOR_BGR2GRAY)
```

### 基于颜色的分割
```python
# 定义颜色范围 (HSV)
lower = np.array([35, 100, 100])
upper = np.array([85, 255, 255])

# 创建掩码
mask = cv2.inRange(hsv, lower, upper)

# 应用掩码
result = cv2.bitwise_and(img, img, mask=mask)
```

## 练习

1. 将图像从 BGR 转换为 RGB，用 Matplotlib 显示
2. 使用 HSV 提取图像中的特定颜色区域
3. 分离图像的 BGR 三个通道并分别显示

## 待创建文件

- `01_color_conversion.py` - 颜色空间转换
- `02_color_segmentation.py` - 基于颜色的分割

